export enum Sort {
  Price = 'sortPrice',
  Popular = 'sortPopular',
}

export enum SortCount {
  Up = 'up',
  Down = 'down',
}

export enum Param {
  sortType = 'sortType',
  sortCount = 'sortCount',
  page = 'page',
}
